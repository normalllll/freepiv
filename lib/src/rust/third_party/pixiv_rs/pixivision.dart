// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';
import 'error.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

Future<Article> parseArticle({required String html, required String url}) =>
    RustLib.instance.api.pixivRsPixivisionParseArticle(html: html, url: url);

Future<ArticlePage> parseArticlePage({
  required String html,
  required String url,
}) => RustLib.instance.api.pixivRsPixivisionParseArticlePage(
  html: html,
  url: url,
);

Future<TagDirectory> parseTagDirectory({
  required String html,
  required String url,
}) => RustLib.instance.api.pixivRsPixivisionParseTagDirectory(
  html: html,
  url: url,
);

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<ArticleFeed>>
abstract class ArticleFeed implements RustOpaqueInterface {}

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<PixivisionApi>>
abstract class PixivisionApi implements RustOpaqueInterface {
  Future<Article> getArticle({required int id});

  /// Also accepts translated article URLs and links to subsequent article pages.
  Future<Article> getArticleByUrl({required String url});

  Future<ArticlePage> getArticlePage({
    required ArticleFeed feed,
    required int page,
  });

  /// Accepts site list links; keeps search/tag/category and pagination parameters.
  Future<ArticlePage> getNextArticlePage({required String url});

  Future<TagDirectory> getTagDirectory();

  // HINT: Make it `#[frb(sync)]` to let it become the default constructor of Dart class.
  static Future<PixivisionApi> newInstance({
    required PixivisionConfig config,
  }) => RustLib.instance.api.pixivRsPixivisionPixivisionApiNew(config: config);
}

class Article {
  final int id;
  final String url;
  final Language language;
  final String title;
  final String? description;
  final String? thumbnail;
  final String publishDate;
  final ArticleLink? category;
  final List<PixivisionTag> tags;
  final List<ArticleLink> translations;

  /// Source order is retained, including unknown block types and article cards.
  final List<ArticleBlock> blocks;
  final List<ArticleSection> related;
  final List<ArticleSummary> monthlyRanking;
  final List<ArticleSummary> recommended;
  final String? nextUrl;
  final String? previousUrl;

  const Article({
    required this.id,
    required this.url,
    required this.language,
    required this.title,
    this.description,
    this.thumbnail,
    required this.publishDate,
    this.category,
    required this.tags,
    required this.translations,
    required this.blocks,
    required this.related,
    required this.monthlyRanking,
    required this.recommended,
    this.nextUrl,
    this.previousUrl,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      url.hashCode ^
      language.hashCode ^
      title.hashCode ^
      description.hashCode ^
      thumbnail.hashCode ^
      publishDate.hashCode ^
      category.hashCode ^
      tags.hashCode ^
      translations.hashCode ^
      blocks.hashCode ^
      related.hashCode ^
      monthlyRanking.hashCode ^
      recommended.hashCode ^
      nextUrl.hashCode ^
      previousUrl.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Article &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          url == other.url &&
          language == other.language &&
          title == other.title &&
          description == other.description &&
          thumbnail == other.thumbnail &&
          publishDate == other.publishDate &&
          category == other.category &&
          tags == other.tags &&
          translations == other.translations &&
          blocks == other.blocks &&
          related == other.related &&
          monthlyRanking == other.monthlyRanking &&
          recommended == other.recommended &&
          nextUrl == other.nextUrl &&
          previousUrl == other.previousUrl;
}

/// HTML is sanitized; links and media URLs are absolute. Embeds are data, not
/// executable HTML. The typed assets also support native cards and media viewers.
class ArticleBlock {
  final BlockKind kind;
  final String? anchor;
  final int? headingLevel;
  final String html;
  final String text;
  final List<ArticleImage> images;
  final List<ArticleLink> links;
  final List<FeaturedWork> works;
  final List<ArticleEmbed> embeds;

  const ArticleBlock({
    required this.kind,
    this.anchor,
    this.headingLevel,
    required this.html,
    required this.text,
    required this.images,
    required this.links,
    required this.works,
    required this.embeds,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      anchor.hashCode ^
      headingLevel.hashCode ^
      html.hashCode ^
      text.hashCode ^
      images.hashCode ^
      links.hashCode ^
      works.hashCode ^
      embeds.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleBlock &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          anchor == other.anchor &&
          headingLevel == other.headingLevel &&
          html == other.html &&
          text == other.text &&
          images == other.images &&
          links == other.links &&
          works == other.works &&
          embeds == other.embeds;
}

class ArticleEmbed {
  final EmbedKind kind;
  final String url;
  final String? title;
  final String? poster;

  const ArticleEmbed({
    required this.kind,
    required this.url,
    this.title,
    this.poster,
  });

  @override
  int get hashCode =>
      kind.hashCode ^ url.hashCode ^ title.hashCode ^ poster.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleEmbed &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          url == other.url &&
          title == other.title &&
          poster == other.poster;
}

class ArticleImage {
  final String url;
  final String alt;
  final int? width;
  final int? height;

  const ArticleImage({
    required this.url,
    required this.alt,
    this.width,
    this.height,
  });

  @override
  int get hashCode =>
      url.hashCode ^ alt.hashCode ^ width.hashCode ^ height.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleImage &&
          runtimeType == other.runtimeType &&
          url == other.url &&
          alt == other.alt &&
          width == other.width &&
          height == other.height;
}

class ArticleLink {
  final String title;
  final String url;

  const ArticleLink({required this.title, required this.url});

  @override
  int get hashCode => title.hashCode ^ url.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleLink &&
          runtimeType == other.runtimeType &&
          title == other.title &&
          url == other.url;
}

class ArticlePage {
  final String url;
  final String title;
  final String? description;
  final List<ArticleSummary> articles;
  final String? nextUrl;
  final String? previousUrl;
  final List<ArticleSummary> monthlyRanking;
  final List<ArticleSummary> recommended;
  final List<ArticleLink> categories;

  const ArticlePage({
    required this.url,
    required this.title,
    this.description,
    required this.articles,
    this.nextUrl,
    this.previousUrl,
    required this.monthlyRanking,
    required this.recommended,
    required this.categories,
  });

  @override
  int get hashCode =>
      url.hashCode ^
      title.hashCode ^
      description.hashCode ^
      articles.hashCode ^
      nextUrl.hashCode ^
      previousUrl.hashCode ^
      monthlyRanking.hashCode ^
      recommended.hashCode ^
      categories.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticlePage &&
          runtimeType == other.runtimeType &&
          url == other.url &&
          title == other.title &&
          description == other.description &&
          articles == other.articles &&
          nextUrl == other.nextUrl &&
          previousUrl == other.previousUrl &&
          monthlyRanking == other.monthlyRanking &&
          recommended == other.recommended &&
          categories == other.categories;
}

class ArticleSection {
  final String title;
  final String? url;
  final List<ArticleSummary> articles;

  const ArticleSection({required this.title, this.url, required this.articles});

  @override
  int get hashCode => title.hashCode ^ url.hashCode ^ articles.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleSection &&
          runtimeType == other.runtimeType &&
          title == other.title &&
          url == other.url &&
          articles == other.articles;
}

class ArticleSummary {
  final int id;
  final String title;
  final String url;
  final String? thumbnail;
  final String? publishDate;
  final ArticleLink? category;
  final List<PixivisionTag> tags;

  const ArticleSummary({
    required this.id,
    required this.title,
    required this.url,
    this.thumbnail,
    this.publishDate,
    this.category,
    required this.tags,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      title.hashCode ^
      url.hashCode ^
      thumbnail.hashCode ^
      publishDate.hashCode ^
      category.hashCode ^
      tags.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ArticleSummary &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          title == other.title &&
          url == other.url &&
          thumbnail == other.thumbnail &&
          publishDate == other.publishDate &&
          category == other.category &&
          tags == other.tags;
}

enum BlockKind {
  paragraph,
  heading,
  image,
  pixivWork,
  video,
  quote,
  list,
  table,
  code,
  tableOfContents,
  profile,
  question,
  answer,
  articleCard,
  divider,
  unknown,
}

enum Category {
  illustration,
  manga,
  novel,
  tutorial,
  making,
  materials,
  interview,
  column,
  news;

  Future<void> path() =>
      RustLib.instance.api.pixivRsPixivisionCategoryPath(that: this);
}

enum EmbedKind { frame, video, audio, socialPost }

class FeaturedWork {
  final int id;
  final FeaturedWorkKind kind;
  final String title;
  final String url;
  final int? userId;
  final String? userName;
  final String? preview;
  final int? pageCount;

  const FeaturedWork({
    required this.id,
    required this.kind,
    required this.title,
    required this.url,
    this.userId,
    this.userName,
    this.preview,
    this.pageCount,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      kind.hashCode ^
      title.hashCode ^
      url.hashCode ^
      userId.hashCode ^
      userName.hashCode ^
      preview.hashCode ^
      pageCount.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FeaturedWork &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          kind == other.kind &&
          title == other.title &&
          url == other.url &&
          userId == other.userId &&
          userName == other.userName &&
          preview == other.preview &&
          pageCount == other.pageCount;
}

enum FeaturedWorkKind { illustration, novel }

enum Language {
  japanese,
  english,
  simplifiedChinese,
  traditionalChinese,
  korean,
  thai,
  malay;

  Future<void> path() =>
      RustLib.instance.api.pixivRsPixivisionLanguagePath(that: this);
}

class PixivisionConfig {
  final Language language;
  final String? proxy;
  final bool acceptInvalidCerts;

  const PixivisionConfig({
    required this.language,
    this.proxy,
    required this.acceptInvalidCerts,
  });

  static Future<PixivisionConfig> default_() =>
      RustLib.instance.api.pixivRsPixivisionPixivisionConfigDefault();

  @override
  int get hashCode =>
      language.hashCode ^ proxy.hashCode ^ acceptInvalidCerts.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PixivisionConfig &&
          runtimeType == other.runtimeType &&
          language == other.language &&
          proxy == other.proxy &&
          acceptInvalidCerts == other.acceptInvalidCerts;
}

class PixivisionTag {
  final int id;
  final String name;
  final String url;

  const PixivisionTag({
    required this.id,
    required this.name,
    required this.url,
  });

  @override
  int get hashCode => id.hashCode ^ name.hashCode ^ url.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PixivisionTag &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          url == other.url;
}

class SpotlightArticle {
  final int id;
  final String title;
  final String? pureTitle;
  final String thumbnail;
  final String articleUrl;
  final String publishDate;
  final String category;
  final String? subcategoryLabel;

  const SpotlightArticle({
    required this.id,
    required this.title,
    this.pureTitle,
    required this.thumbnail,
    required this.articleUrl,
    required this.publishDate,
    required this.category,
    this.subcategoryLabel,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      title.hashCode ^
      pureTitle.hashCode ^
      thumbnail.hashCode ^
      articleUrl.hashCode ^
      publishDate.hashCode ^
      category.hashCode ^
      subcategoryLabel.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpotlightArticle &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          title == other.title &&
          pureTitle == other.pureTitle &&
          thumbnail == other.thumbnail &&
          articleUrl == other.articleUrl &&
          publishDate == other.publishDate &&
          category == other.category &&
          subcategoryLabel == other.subcategoryLabel;
}

enum SpotlightCategory { all, manga }

class SpotlightPage {
  final List<SpotlightArticle> spotlightArticles;
  final String? nextUrl;

  const SpotlightPage({required this.spotlightArticles, this.nextUrl});

  @override
  int get hashCode => spotlightArticles.hashCode ^ nextUrl.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpotlightPage &&
          runtimeType == other.runtimeType &&
          spotlightArticles == other.spotlightArticles &&
          nextUrl == other.nextUrl;
}

class TagDirectory {
  final String url;
  final List<TagGroup> groups;

  const TagDirectory({required this.url, required this.groups});

  @override
  int get hashCode => url.hashCode ^ groups.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TagDirectory &&
          runtimeType == other.runtimeType &&
          url == other.url &&
          groups == other.groups;
}

class TagGroup {
  final String name;
  final List<TagNode> nodes;

  const TagGroup({required this.name, required this.nodes});

  @override
  int get hashCode => name.hashCode ^ nodes.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TagGroup &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          nodes == other.nodes;
}

class TagNode {
  final String name;
  final PixivisionTag? tag;
  final int? articleCount;
  final List<TagNode> children;

  const TagNode({
    required this.name,
    this.tag,
    this.articleCount,
    required this.children,
  });

  @override
  int get hashCode =>
      name.hashCode ^ tag.hashCode ^ articleCount.hashCode ^ children.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TagNode &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          tag == other.tag &&
          articleCount == other.articleCount &&
          children == other.children;
}
